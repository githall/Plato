@model LayoutViewModel
@{
    AddTitleSegment(T["Ideas"]);
    AddTitleSegment(T["Popular"]);
}

<form asp-action="Popular"  class="idea-popular" method="post" enctype="multipart/form-data" data-disable-submit="true">
    <layout model="Model" />
</form>