@model LayoutViewModel
@{
    AddTitleSegment(T["Ideas"]);
    AddTitleSegment(T["New Idea"]);
}

<form asp-action="Create" class="idea-create" method="post" enctype="multipart/form-data" data-disable-submit="true">
    <layout model="Model" />
</form>