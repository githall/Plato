@model LayoutViewModel
@{
    AddTitleSegment(T["Questions"]);
    AddTitleSegment(T["Edit Answer"]);
}

<form asp-action="EditReply" class="question-edit-reply" method="post" enctype="multipart/form-data" data-disable-submit="true">
    <layout model="Model" />
</form>