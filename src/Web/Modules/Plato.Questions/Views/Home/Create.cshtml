@model LayoutViewModel
@{
    AddTitleSegment(T["Questions"]);
    AddTitleSegment(T["New Question"]);
}

<form asp-action="Create" class="question-create" method="post" enctype="multipart/form-data" data-disable-submit="true">
    <layout model="Model" />
</form>