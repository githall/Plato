@model LayoutViewModel
@{
    AddTitleSegment(T["Questions"]);
    AddTitleSegment(T["Popular"]);
}

<form asp-action="Popular" class="question-popular" method="post" enctype="multipart/form-data" data-disable-submit="true">
    <layout model="Model" />
</form>