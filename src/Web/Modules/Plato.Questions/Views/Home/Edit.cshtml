@model LayoutViewModel
@{
    AddTitleSegment(T["Questions"]);
    AddTitleSegment(T["Edit Question"]);
}

<form asp-action="Edit" class="question-edit" method="post" enctype="multipart/form-data" data-disable-submit="true">
    <layout model="Model" />
</form>