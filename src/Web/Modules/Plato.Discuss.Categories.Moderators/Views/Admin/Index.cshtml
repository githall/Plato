@model LayoutViewModel
@{
    AddTitleSegment(T["Admin"]);
    AddTitleSegment(T["Discuss"]);
    AddTitleSegment(T["Moderators"]);
}

<form asp-action="Index" method="post" enctype="multipart/form-data" data-disable-submit="true">
    <layout model="Model" />
</form>