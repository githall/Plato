@model TourStep

<card>
    <card-body>
        @await DisplayAsync(new View("TourFeatureList", new
        {
            options = new FeatureIndexOptions()
            {
                ModuleIds = ShellDescriptors.CoreModules,
                HideEnabled = true
            }
        }))
    </card-body>
</card>
