@model TourStep

<card>
    <card-body>
        @await DisplayAsync(new View("TourFeatureList", new
        {
            options = new FeatureIndexOptions()
            {
                ModuleIds = ShellDescriptors.SearchModules,
                HideEnabled = true
            }
        }))
    </card-body>
</card>
