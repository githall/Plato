@model LayoutViewModel
@{
    AddTitleSegment(T["Ideas"]);
    AddTitleSegment(T["Labels"]);
}

<form asp-action="Display"  class="idea-labels-display layout-header-sticky" method="post" enctype="multipart/form-data" data-disable-submit="true">
    <layout model="Model" />
</form>