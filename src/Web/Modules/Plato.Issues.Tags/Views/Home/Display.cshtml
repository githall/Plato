@model LayoutViewModel
@{
    AddTitleSegment(T["Issues"]);
    AddTitleSegment(T["Tags"]);
}

<form asp-action="Display" class="issue-tags-display" method="post" enctype="multipart/form-data" data-disable-submit="true" role="form">
    <layout model="Model" />
</form>