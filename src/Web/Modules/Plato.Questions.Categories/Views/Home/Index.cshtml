@model LayoutViewModel
@{
    AddTitleSegment(T["Questions"]);
    AddTitleSegment(T["Categories"]);
}

<form asp-action="Index" class="question-categories-index" method="post" enctype="multipart/form-data" data-disable-submit="true" role="form">
    <layout model="Model" />
</form>