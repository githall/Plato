@model LayoutViewModel
@{
    AddTitleSegment(T["Discuss"]);
    AddTitleSegment(T["Edit Topic"]);
}

<form asp-action="Edit" class="discuss-edit" method="post" enctype="multipart/form-data" data-disable-submit="true">
    <layout model="Model" />
</form>