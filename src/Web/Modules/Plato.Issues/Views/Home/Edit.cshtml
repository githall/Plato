@model LayoutViewModel
@{
    AddTitleSegment(T["Issues"]);
    AddTitleSegment(T["Edit Issue"]);
}

<form asp-action="Edit" class="issue-edit" method="post" enctype="multipart/form-data" data-disable-submit="true">
    <layout model="Model" />
</form>