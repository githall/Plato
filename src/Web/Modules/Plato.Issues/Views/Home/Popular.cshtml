@model LayoutViewModel
@{
    AddTitleSegment(T["Articles"]);
    AddTitleSegment(T["Popular"]);
}

<form asp-action="Popular" class="issue-popular" method="post" enctype="multipart/form-data" data-disable-submit="true">
    <layout model="Model" />
</form>