@model LayoutViewModel
@{
    AddTitleSegment(T["Badges"]);
}

<form asp-action="Index" class="badge-index" method="post" enctype="multipart/form-data" data-disable-submit="true">
    <layout model="Model" />
</form>