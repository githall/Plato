@model LayoutViewModel
@{
    AddTitleSegment(T["Edit Settings"]);
}

<form asp-action="EditSettings" class="user-edit-settings" method="post" enctype="multipart/form-data" data-disable-submit="true" role="form">
    <layout model="Model" />
</form>