@model LayoutViewModel
@{
    AddTitleSegment(T["Users"]);
}

<form asp-action="Index" class="user-index" method="post" enctype="multipart/form-data" data-disable-submit="true" role="form">
    <layout model="Model" />
</form>