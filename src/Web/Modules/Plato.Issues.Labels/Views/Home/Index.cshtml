@model LayoutViewModel
@{
    AddTitleSegment(T["Issues"]);
    AddTitleSegment(T["Labels"]);
}

<form asp-action="Index" class="issue-labels-index layout-header-sticky" method="post" enctype="multipart/form-data" data-disable-submit="true" role="form">
    <layout model="Model" />
</form>