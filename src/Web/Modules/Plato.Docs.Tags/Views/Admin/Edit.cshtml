@model LayoutViewModel
@{
    AddTitleSegment(T["Admin"]);
    AddTitleSegment(T["Docs"]);
    AddTitleSegment(T["Tags"]);
    AddTitleSegment(T["Edit Tag"]);
}

<form asp-action="Edit" method="post" enctype="multipart/form-data" data-disable-submit="true">
    <layout model="Model" />
</form>
