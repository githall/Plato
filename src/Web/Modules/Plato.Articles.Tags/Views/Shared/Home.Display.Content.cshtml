@model TagDisplayViewModel

@await DisplayAsync(new View("ArticleList", new
{
    model = new EntityIndexViewModel<Article>()
    {
        Options = Model.Options,
        Pager = Model.Pager
    }     
}))
