@model LayoutViewModel
@{
    AddTitleSegment(T["Admin"]);
    AddTitleSegment(T["Tenants"]);
    AddTitleSegment(T["Settings"]);
}

<form asp-action="Settings" method="post" enctype="multipart/form-data" data-disable-submit="true">
    <layout model="Model" />
</form>