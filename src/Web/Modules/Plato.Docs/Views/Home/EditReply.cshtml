@model LayoutViewModel
@{
    AddTitleSegment(T["Docs"]);
    AddTitleSegment(T["Edit Comment"]);
}

<form asp-action="EditReply" class="doc-edit-reply" method="post" enctype="multipart/form-data" data-disable-submit="true">
    <layout model="Model" />
</form>