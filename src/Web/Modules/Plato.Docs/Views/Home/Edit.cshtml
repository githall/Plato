@model LayoutViewModel
@{
    AddTitleSegment(T["Docs"]);
    AddTitleSegment(T["Edit Doc"]);
}

<form asp-action="Edit" class="doc-edit" method="post" enctype="multipart/form-data" data-disable-submit="true">
    <layout model="Model" />
</form>